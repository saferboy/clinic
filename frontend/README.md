
  # UI design based on file

  This is a code bundle for UI design based on file. The original project is available at https://www.figma.com/design/pVGmNOad8rdKyT5R5cnYBJ/UI-design-based-on-file.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  