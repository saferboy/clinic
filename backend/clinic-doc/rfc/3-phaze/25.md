# 📋 RFC-025: Boshqaruv Paneli Metrikalari (Dashboard Metrics Management)

---

## 📄 RFC METADATA

| Maydon | Qiymat |
|--------|--------|
| **RFC ID** | RFC-025 |
| **Nomi** | Dashboard Metrics Management |
| **Phase** | 3 - Reports & Analytics (Oxirgi) |
| **Model** | `Report` (Virtual - mavjud ma'lumotlardan hosil qilinadi) |
| **Status** | Draft |
| **Yaratilgan** | 2024-01-15 |
| **Muallif** | Senior Backend Developer |
| **Tasdiqlangan** | _______________ |
| **Prioritet** | 🔴 High (Critical Path - Analytics) |
| **Bog'liq RFC** | RFC-019 (Daily Report), RFC-020 (Monthly Report), RFC-021 (Doctor Performance), RFC-022 (Client Report), RFC-023 (Service Report), RFC-024 (Debt Report) |
| **Reference** | `klinika_prisma.txt`, `Klinika.md` (Sections 7.1, 7.2, 8.1, 9.1) |

---

## 1. MAQSAD VA QAMROV

### 1.1 Maqsad
Ushbu RFC klinika boshqaruv paneli (Dashboard) uchun asosiy metrikalarni taqdim etish uchun to'liq texnik specifikatsiyani taqdim etadi. Kunlik, oylik va yillik ko'rsatkichlarni real-time kuzatish, tez qaror qabul qilish uchun vizual ma'lumotlar taqdim etish. Tizimning barcha modullaridan ma'lumotlarni agregatsiya qilish va foydalanuvchi roli bo'yicha filtrlash (Reference: `Klinika.md` 7.1, 7.2).

### 1.2 Qamrov
| Doxil | Doxil Emas |
|-------|------------|
| ✅ Dashboard KPI metrikalarini olish | ❌ Frontend implementatsiya |
| ✅ Rol bo'yicha filterlash | ❌ Mobile integratsiya |
| ✅ Vaqt oralig'i filtrlari | ❌ Real-time WebSocket |
| ✅ Dashboard export (PDF/Excel) | ❌ Avtomatik email yuborish |
| ✅ Tezkor xabarlar (Alerts) | |
| ✅ Grafiklar uchun ma'lumotlar | |

### 1.3 Biznes Qiymati (Reference: `Klinika.md` 1.2, 7.1, 7.2)
- Boshqaruv qarorlarini tezkor qabul qilish
- Klinika ko'rsatkichlarini real-time kuzatish
- Muammolarni erta aniqlash
- Shifokorlar va bo'limlar samaradorligini baholash
- Moliyaviy holatni monitoring qilish
- Operatsion metrikalarni optimallashtirish

---

## 2. MA'LUMOTLAR MODELI

### 2.1 Virtual Report Model (Reference: `klinika_prisma.txt`)

Dashboard alohida jadvalda saqlanmaydi. U quyidagi jadvallardan real-time hosil qilinadi:

```
┌─────────────────────────────────────────────────────────────┐
│              DASHBOARD DATA SOURCES                         │
├─────────────────────────────────────────────────────────────┤
│  Visit          → Qabullar statistikasi                     │
│  Client         → Mijozlar statistikasi                     │
│  Payment        → To'lovlar va kirim                        │
│  Service        → Xizmatlar taqsimoti                       │
│  Room           → Xona bandligi                             │
│  User           → Shifokorlar yuklamasi                     │
│  Department     → Bo'limlar ko'rsatkichlari                 │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 Report Tuzilishi

```typescript
interface DashboardMetrics {
  period: {
    from: Date;
    to: Date;
    type: 'today' | 'week' | 'month' | 'year' | 'custom';
  };
  kpis: KPIs;
  charts: Charts;
  alerts: Alerts;
  lastUpdated: Date;
}

interface KPIs {
  totalVisits: number;           // Jami visitlar
  completedVisits: number;       // Yakunlangan visitlar
  totalRevenue: number;          // Jami kirim
  averageCheck: number;          // O'rtacha check
  retentionRate: number;         // Mijoz qaytish foizi
  occupancyRate: number;         // Xona bandligi
  debtRate: number;              // Qarzdorlik foizi
  totalClients: number;          // Jami mijozlar
  newClients: number;            // Yangi mijozlar
  totalDoctors: number;          // Faol shifokorlar
}

interface Charts {
  visitTrend: VisitTrendData[];      // Visit trendi
  revenueTrend: RevenueTrendData[];  // Kirim trendi
  serviceDistribution: ServiceDist[]; // Xizmat taqsimoti
  doctorPerformance: DoctorPerf[];    // Shifokor ko'rsatkichlari
}

interface Alerts {
  scheduledVisits: number;       // Bugungi rejalashtirilgan
  debtClients: number;           // Qarzдор mijozlar
  availableRooms: number;        // Bo'sh xonalar
  pendingPayments: number;       // Tugallanmagan to'lovlar
}

interface VisitTrendData {
  date: Date;
  count: number;
  revenue: number;
}

interface RevenueTrendData {
  date: Date;
  income: number;
  outcome: number;
  net: number;
}

interface ServiceDist {
  serviceId: number;
  serviceName: string;
  count: number;
  revenue: number;
  percentage: number;
}

interface DoctorPerf {
  doctorId: number;
  doctorName: string;
  visitCount: number;
  revenue: number;
  commission: number;
}
```

### 2.3 Ma'lumotlar Oqimi

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   Database  │ →  │  Calculator │ →  │   Report    │
│   Queries   │    │   Service   │    │   JSON      │
└─────────────┘    └─────────────┘    └─────────────┘
       │                  │                  │
       ▼                  ▼                  ▼
Visit, Client,    Aggregate,      Cache Store
Payment, Service  Calculate       (5 min TTL)
Room, User        Metrics
```

---

## 3. API SPECIFIKATSIYASI

### 3.1 Endpoint'lar Ro'yxati

| # | Method | Endpoint | Auth | Rol | Tavsif |
|---|--------|----------|------|-----|--------|
| 1 | GET | `/api/v1/dashboard/metrics` | ✅ JWT | Barcha (rol bo'yicha filter) | Dashboard metrikalarini olish |
| 2 | POST | `/api/v1/dashboard/refresh` | ✅ JWT | Admin, Accountant | Dashboard ma'lumotlarini yangilash |
| 3 | POST | `/api/v1/dashboard/export` | ✅ JWT | Admin, Accountant | Dashboard export (PDF/Excel) |
| 4 | GET | `/api/v1/dashboard/alerts` | ✅ JWT | Barcha | Tezkor xabarlar |

---

### 3.2 GET /api/v1/dashboard/metrics

**Tavsif:** Dashboard metrikalarini olish (Barcha rollar, rol bo'yicha filter)

**Request Headers:**
```http
Authorization: Bearer <jwt_token>
```

**Query Parameters:**
| Param | Tip | Majburiy | Default | Tavsif |
|-------|-----|----------|---------|--------|
| `period` | string | ❌ | month | Vaqt oralig'i (today/week/month/year/custom) |
| `from` | date | ❌ | - | Boshlanish sanasi (custom uchun) |
| `to` | date | ❌ | - | Tugash sanasi (custom uchun) |

**Request Example:**
```http
GET /api/v1/dashboard/metrics?period=month
```

**Response (200 OK):**
```json
{
  "success": true,
  "data": {
    "period": {
      "from": "2024-01-01T00:00:00.000Z",
      "to": "2024-01-31T23:59:59.999Z",
      "type": "month"
    },
    "kpis": {
      "totalVisits": 1350,
      "completedVisits": 1140,
      "totalRevenue": 450000000,
      "averageCheck": 333333,
      "retentionRate": 65.5,
      "occupancyRate": 75.0,
      "debtRate": 16.7,
      "totalClients": 1200,
      "newClients": 280,
      "totalDoctors": 15
    },
    "charts": {
      "visitTrend": [
        {
          "date": "2024-01-01T00:00:00.000Z",
          "count": 45,
          "revenue": 15000000
        }
      ],
      "revenueTrend": [...],
      "serviceDistribution": [...],
      "doctorPerformance": [...]
    },
    "alerts": {
      "scheduledVisits": 45,
      "debtClients": 30,
      "availableRooms": 3,
      "pendingPayments": 25
    },
    "lastUpdated": "2024-02-01T10:00:00.000Z"
  },
  "cached": true
}
```

**Service Layer Implementation:**
```typescript
// dashboard.service.ts
async getDashboardMetrics(
  periodType: 'today' | 'week' | 'month' | 'year' | 'custom',
  customRange?: { from: Date; to: Date },
  currentUserId: number,
  currentUserRole: string
): Promise<DashboardMetrics> {
  // 1. Vaqt oralig'ini aniqlash
  const { from, to } = this.getDateRange(periodType, customRange);

  // 2. Rol bo'yicha filter
  const filters = this.getRoleFilters(currentUserId, currentUserRole);

  // 3. Barcha metrikalarni parallel hisoblash
  const [
    kpis,
    charts,
    alerts
  ] = await Promise.all([
    this.calculateKPIs(from, to, filters),
    this.calculateCharts(from, to, filters),
    this.calculateAlerts(from, filters)
  ]);

  return {
    period: {
      from,
      to,
      type: periodType
    },
    kpis,
    charts,
    alerts,
    lastUpdated: new Date()
  };
}

// KPI metrikalarini hisoblash
private async calculateKPIs(
  from: Date,
  to: Date,
  filters: any
): Promise<KPIs> {
  const [
    visitStats,
    paymentStats,
    clientStats,
    roomStats,
    debtStats
  ] = await Promise.all([
    this.getVisitStats(from, to, filters),
    this.getPaymentStats(from, to, filters),
    this.getClientStats(from, to, filters),
    this.getRoomStats(from, to, filters),
    this.getDebtStats(from, to, filters)
  ]);

  // O'rtacha check
  const averageCheck = visitStats.totalVisits > 0
    ? Math.round((visitStats.totalRevenue / visitStats.totalVisits) * 100) / 100
    : 0;

  // Mijoz qaytish foizi (Retention Rate)
  const retentionRate = await this.calculateRetentionRate(from, to, filters);

  // Xona bandligi (Occupancy Rate)
  const occupancyRate = roomStats.totalRooms > 0
    ? Math.round((roomStats.occupiedRooms / roomStats.totalRooms) * 100 * 10) / 10
    : 0;

  // Qarzdorlik foizi (Debt Rate)
  const debtRate = visitStats.totalRevenue > 0
    ? Math.round((debtStats.totalDebt / visitStats.totalRevenue) * 100 * 10) / 10
    : 0;

  return {
    totalVisits: visitStats.totalVisits,
    completedVisits: visitStats.completedVisits,
    totalRevenue: paymentStats.totalRevenue,
    averageCheck,
    retentionRate,
    occupancyRate,
    debtRate,
    totalClients: clientStats.totalClients,
    newClients: clientStats.newClients,
    totalDoctors: clientStats.activeDoctors
  };
}

// Vaqt oralig'ini aniqlash
private getDateRange(
  periodType: string,
  customRange?: { from: Date; to: Date }
): { from: Date; to: Date } {
  const now = new Date();
  let from = new Date();
  const to = now;

  switch (periodType) {
    case 'today':
      from.setHours(0, 0, 0, 0);
      break;
    case 'week':
      from.setDate(now.getDate() - 7);
      break;
    case 'month':
      from.setMonth(now.getMonth() - 1);
      break;
    case 'year':
      from.setFullYear(now.getFullYear() - 1);
      break;
    case 'custom':
      from = customRange?.from || from;
      break;
  }

  return { from, to };
}

// Rol bo'yicha filter
private getRoleFilters(userId: number, role: string): any {
  const filters: any = {};

  if (role === 'Doctor') {
    filters.doctor_id = userId;
  }

  return filters;
}

// Mijoz qaytish foizi
private async calculateRetentionRate(
  from: Date,
  to: Date,
  filters: any
): Promise<number> {
  // Oldingi davr mijozlari
  const previousFrom = new Date(from);
  previousFrom.setDate(previousFrom.getDate() - 30);
  
  const previousClients = await this.prisma.client.findMany({
    where: {
      created_at: {
        gte: previousFrom,
        lt: from
      },
      deleted_at: null,
      ...filters
    },
    select: { id: true }
  });

  // Joriy davrda qaytgan mijozlar
  const returnedClients = await this.prisma.visit.count({
    where: {
      visit_date: {
        gte: from,
        lt: to
      },
      client_id: {
        in: previousClients.map(c => c.id)
      },
      deleted_at: null,
      ...filters
    },
    distinct: ['client_id']
  });

  return previousClients.length > 0
    ? Math.round((returnedClients / previousClients.length) * 100 * 10) / 10
    : 0;
}
```

---

### 3.3 POST /api/v1/dashboard/refresh

**Tavsif:** Dashboard ma'lumotlarini yangilash (Admin, Accountant)

**Request Headers:**
```http
Authorization: Bearer <jwt_token>
Content-Type: application/json
```

**Response (200 OK):**
```json
{
  "success": true,
  "message": "Dashboard ma'lumotlari yangilandi",
  "data": {
    "refreshedAt": "2024-02-01T10:00:00.000Z"
  }
}
```

**Service Layer Implementation:**
```typescript
async refreshDashboard(userId: number): Promise<{ refreshedAt: Date }> {
  // Cache ni tozalash
  await this.invalidateDashboardCache();
  
  return {
    refreshedAt: new Date()
  };
}

async invalidateDashboardCache(): Promise<void> {
  const pattern = 'dashboard:*';
  const keys = await this.cacheService.keys(pattern);
  
  if (keys.length > 0) {
    await this.cacheService.del(keys);
    this.logger.log(`Dashboard cache invalidated: ${keys.length} keys`);
  }
}
```

---

### 3.4 POST /api/v1/dashboard/export

**Tavsif:** Dashboard ma'lumotlarini export qilish (PDF/Excel) (Admin, Accountant)

**Request Headers:**
```http
Authorization: Bearer <jwt_token>
Content-Type: application/json
```

**Request Body:**
```typescript
interface ExportDashboardDto {
  period: 'today' | 'week' | 'month' | 'year' | 'custom';
  from?: Date;
  to?: Date;
  format: 'pdf' | 'excel';
  includeCharts?: boolean;
}
```

**Request Body Example:**
```json
{
  "period": "month",
  "format": "pdf",
  "includeCharts": true
}
```

**Response:** File Download
- Excel: `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`
- PDF: `application/pdf`

**Service Layer Implementation:**
```typescript
async exportDashboard(
  exportDto: ExportDashboardDto,
  userId: number,
  currentUserRole: string
): Promise<Buffer> {
  // 1. Dashboard ma'lumotlarini olish
  const metrics = await this.getDashboardMetrics(
    exportDto.period,
    exportDto.from && exportDto.to ? { from: exportDto.from, to: exportDto.to } : undefined,
    userId,
    currentUserRole
  );
  
  // 2. Formatga qarab export qilish
  if (exportDto.format === 'excel') {
    return this.generateExcel(metrics, exportDto.includeCharts);
  } else if (exportDto.format === 'pdf') {
    return this.generatePDF(metrics, exportDto.includeCharts);
  }
  
  throw new BadRequestException('DB_006');
}

// Excel generatsiya
private async generateExcel(
  metrics: DashboardMetrics,
  includeCharts: boolean
): Promise<Buffer> {
  const workbook = new ExcelJS.Workbook();
  workbook.properties.created = new Date();
  workbook.properties.creator = 'Klinika CRM';
  
  // KPI sheet
  const kpiSheet = workbook.addWorksheet('KPIs');
  kpiSheet.columns = [
    { header: 'Ko\'rsatkich', key: 'metric', width: 30 },
    { header: 'Qiymat', key: 'value', width: 20 }
  ];
  kpiSheet.addRows([
    { metric: 'Jami Visitlar', value: metrics.kpis.totalVisits },
    { metric: 'Yakunlangan Visitlar', value: metrics.kpis.completedVisits },
    { metric: 'Jami Kirim', value: metrics.kpis.totalRevenue },
    { metric: 'O\'rtacha Check', value: metrics.kpis.averageCheck },
    { metric: 'Mijoz Qaytish Foizi', value: `${metrics.kpis.retentionRate}%` },
    { metric: 'Xona Bandligi', value: `${metrics.kpis.occupancyRate}%` },
    { metric: 'Qarzdorlik Foizi', value: `${metrics.kpis.debtRate}%` },
    { metric: 'Jami Mijozlar', value: metrics.kpis.totalClients },
    { metric: 'Yangi Mijozlar', value: metrics.kpis.newClients },
    { metric: 'Faol Shifokorlar', value: metrics.kpis.totalDoctors }
  ]);
  
  // Alerts sheet
  const alertsSheet = workbook.addWorksheet('Tezkor Xabarlar');
  alertsSheet.columns = [
    { header: 'Xabar', key: 'alert', width: 30 },
    { header: 'Son', key: 'count', width: 15 }
  ];
  alertsSheet.addRows([
    { alert: 'Rejalashtirilgan Visitlar', count: metrics.alerts.scheduledVisits },
    { alert: 'Qarzдор Mijozlar', count: metrics.alerts.debtClients },
    { alert: 'Bo\'sh Xonalar', count: metrics.alerts.availableRooms },
    { alert: 'Tugallanmagan To\'lovlar', count: metrics.alerts.pendingPayments }
  ]);
  
  // Buffer ga convert
  return await workbook.xlsx.writeBuffer();
}
```

---

### 3.5 GET /api/v1/dashboard/alerts

**Tavsif:** Tezkor xabarlarni olish (Barcha rollar)

**Response (200 OK):**
```json
{
  "success": true,
  "data": {
    "scheduledVisits": 45,
    "debtClients": 30,
    "availableRooms": 3,
    "pendingPayments": 25,
    "lastUpdated": "2024-02-01T10:00:00.000Z"
  }
}
```

**Service Layer Implementation:**
```typescript
async getAlerts(currentUserId: number, currentUserRole: string): Promise<Alerts> {
  const filters = this.getRoleFilters(currentUserId, currentUserRole);
  return await this.calculateAlerts(new Date(), filters);
}

private async calculateAlerts(
  from: Date,
  filters: any
): Promise<Alerts> {
  const now = new Date();
  const todayStart = new Date(now.setHours(0, 0, 0, 0));
  const todayEnd = new Date(now.setHours(23, 59, 59, 999));

  // Bugungi rejalashtirilgan visitlar
  const scheduledVisits = await this.prisma.visit.count({
    where: {
      visit_date: {
        gte: todayStart,
        lt: todayEnd
      },
      status: 'SCHEDULED',
      deleted_at: null,
      ...filters
    }
  });

  // Qarzдор mijozlar
  const debtClients = await this.prisma.client.count({
    where: {
      visits: {
        some: {
          debt_amount: { gt: 0 },
          deleted_at: null
        }
      },
      deleted_at: null,
      ...filters
    }
  });

  // Bo'sh xonalar
  const availableRooms = await this.prisma.room.count({
    where: {
      status: 'AVAILABLE',
      record_status: 'ACTIVE',
      deleted_at: null
    }
  });

  // Tugallanmagan to'lovlar
  const pendingPayments = await this.prisma.visit.count({
    where: {
      debt_amount: { gt: 0 },
      status: 'COMPLETED',
      deleted_at: null,
      ...filters
    }
  });

  return {
    scheduledVisits,
    debtClients,
    availableRooms,
    pendingPayments
  };
}
```

---

## 4. VALIDATSIYA QOIDALARI

### 4.1 Class Validator DTO

```typescript
// get-dashboard-metrics.dto.ts
import {
  IsOptional,
  IsEnum,
  IsDateString
} from 'class-validator';

export enum DashboardPeriod {
  TODAY = 'today',
  WEEK = 'week',
  MONTH = 'month',
  YEAR = 'year',
  CUSTOM = 'custom'
}

export class GetDashboardMetricsDto {
  @IsOptional()
  @IsEnum(DashboardPeriod)
  period?: DashboardPeriod;

  @IsOptional()
  @IsDateString()
  from?: string;

  @IsOptional()
  @IsDateString()
  to?: string;
}

// export-dashboard.dto.ts
import { IsNotEmpty, IsBoolean } from 'class-validator';

export enum ExportFormat {
  PDF = 'pdf',
  EXCEL = 'excel'
}

export class ExportDashboardDto {
  @IsEnum(DashboardPeriod)
  @IsNotEmpty()
  period: DashboardPeriod;

  @IsOptional()
  @IsDateString()
  from?: string;

  @IsOptional()
  @IsDateString()
  to?: string;

  @IsEnum(ExportFormat)
  @IsNotEmpty()
  format: ExportFormat;

  @IsOptional()
  @IsBoolean()
  includeCharts?: boolean;
}
```

### 4.2 Validatsiya Matritsasi

| Maydon | Qoida | Xatolik Kodi | Xabar |
|--------|-------|--------------|-------|
| `period` | Enum | DB_002 | today/week/month/year/custom tanlang |
| `from` | IsDateString | DB_003 | Sana formati noto'g'ri |
| `to` | IsDateString | DB_003 | Sana formati noto'g'ri |
| `to` | >= from | DB_003 | Tugash sana boshlanish sanadan keyin bo'lishi kerak |
| `format` | Enum | DB_006 | pdf/excel tanlang |
| `includeCharts` | IsBoolean | DB_007 | Boolean qiymat bo'lishi kerak |

---

## 5. XATOLIKLAR VA HANDLING

### 5.1 Xatoliklar Jadvali

| Kod | HTTP Status | Xabar | Sabab | Yechim |
|-----|-------------|-------|-------|--------|
| `DB_001` | 403 Forbidden | Ruxsat yo'q | Insufficient permissions | Rol tekshirilsin (Reference: `Klinika.md` 6.1) |
| `DB_002` | 400 Bad Request | Period noto'g'ri | Invalid period type | today/week/month/year/custom tanlang |
| `DB_003` | 400 Bad Request | Sana oralig'i noto'g'ri | Invalid date range | from < to bo'lishi kerak |
| `DB_004` | 404 Not Found | Ma'lumot topilmadi | No data for period | Boshqa period tanlang |
| `DB_005` | 400 Bad Request | Export formati noto'g'ri | Invalid format | pdf/excel tanlang |
| `DB_006` | 400 Bad Request | Tafsilot noto'g'ri | Invalid boolean | true/false tanlang |
| `DB_007` | 500 Internal Server Error | Dashboard ma'lumotlarini olishda xatolik | Calculation error | Admin bilan bog'laning |

### 5.2 Exception Filter

```typescript
// dashboard-exception.filter.ts
@Catch()
export class DashboardExceptionFilter implements ExceptionFilter {
  catch(exception: unknown, host: ArgumentsHost) {
    const ctx = host.switchToHttp();
    const response = ctx.getResponse<Response>();
    const status = exception instanceof HttpException
      ? exception.getStatus()
      : HttpStatus.INTERNAL_SERVER_ERROR;

    const errorResponse = {
      success: false,
      timestamp: new Date().toISOString(),
      status,
      code: this.getErrorCode(exception),
      message: this.getErrorMessage(exception),
    };

    response.status(status).json(errorResponse);
  }

  private getErrorCode(exception: unknown): string {
    if (exception instanceof ForbiddenException) return 'DB_001';
    if (exception instanceof BadRequestException) return 'DB_002';
    if (exception instanceof NotFoundException) return 'DB_004';
    return 'DB_007';
  }
}
```

---

## 6. XAVFSIZLIK TALABLARI (Reference: `Klinika.md` 8)

### 6.1 Autentifikatsiya
- ✅ Barcha endpoint'lar JWT token talab qiladi (Reference: `Klinika.md` 8.1)
- ✅ Token expiry: 24 soat
- ✅ Token validatsiyasi har bir so'rovda

### 6.2 Avtorizatsiya (RBAC) (Reference: `Klinika.md` 6.1)

| Endpoint | Admin | Doctor | Nurse | Receptionist | Accountant |
|----------|-------|--------|-------|--------------|------------|
| GET /dashboard/metrics | ✅ | ✅ (o'zi) | ❌ | ✅ (operatsion) | ✅ (moliyaviy) |
| POST /dashboard/refresh | ✅ | ❌ | ❌ | ❌ | ✅ |
| POST /dashboard/export | ✅ | ❌ | ❌ | ❌ | ✅ |
| GET /dashboard/alerts | ✅ | ✅ | ❌ | ✅ | ✅ |

### 6.3 Audit (Reference: `Klinika.md` 5.2)
| Harakat | Log Qilinadi | Saqlash Muddati |
|--------|-------------|-----------------|
| Dashboard ko'rish | ✅ | 30 kun |
| Export qilish | ✅ | 5 yil |
| Cache access | ✅ | 7 kun |
| Refresh qilish | ✅ | 1 yil |

### 6.4 Ma'lumotlar Xavfsizligi (Reference: `Klinika.md` 8.1)
- ✅ Rol bo'yicha ma'lumotlar filtrlanadi
- ✅ Moliyaviy ma'lumotlar faqat Admin/Accountant uchun
- ✅ Doctor faqat o'z metrikalarini ko'ra oladi
- ✅ Sensitive data maskalanadi
- ✅ Export fayllar vaqtinchalik saqlanadi (24 soat)

---

## 7. CACHE STRATEGY (Reference: `Klinika.md` 9.1)

### 7.1 Caching Configuration

| Data | Cache | TTL | Invalidation |
|------|-------|-----|--------------|
| Dashboard Metrics | Redis | 5 daqiqa | Visit/Payment/Client create/update |
| KPI Metrics | Redis | 5 daqiqa | Real-time events |
| Chart Data | Redis | 10 daqiqa | Period change |
| Alert Data | Redis | 2 daqiqa | Real-time updates |
| Export File | Redis | 24 soat | Automatic expiry |

### 7.2 Cache Implementation

```typescript
// Cache service
async getDashboardMetrics(
  periodType: string,
  userId: number,
  role: string
): Promise<DashboardMetrics> {
  const cacheKey = `dashboard:${periodType}:${userId}:${role}`;
  
  // 1. Cache dan olish
  const cached = await this.cacheService.get(cacheKey);
  if (cached) {
    this.logger.debug(`Cache hit for ${cacheKey}`);
    return cached;
  }
  
  this.logger.debug(`Cache miss for ${cacheKey}`);
  
  // 2. DB dan hisoblash
  const metrics = await this.calculateDashboardMetrics(periodType, userId, role);
  
  // 3. Cache ga saqlash (5 daqiqa)
  await this.cacheService.set(cacheKey, metrics, { ttl: 300 });
  
  return metrics;
}

// Cache invalidation
async invalidateDashboardCache(): Promise<void> {
  const pattern = 'dashboard:*';
  const keys = await this.cacheService.keys(pattern);
  
  if (keys.length > 0) {
    await this.cacheService.del(keys);
    this.logger.log(`Dashboard cache invalidated: ${keys.length} keys`);
  }
}

// Event-based invalidation
@Events('visit.created')
@Events('visit.updated')
@Events('payment.created')
@Events('client.created')
async onResourceChanged(): Promise<void> {
  await this.invalidateDashboardCache();
}
```

---

## 8. PERFORMANCE OPTIMALLASHTIRISH (Reference: `Klinika.md` 9.1)

### 8.1 Database Query Optimization

```typescript
// ✅ Yaxshi - Parallel queries
const [kpis, charts, alerts] = await Promise.all([
  this.calculateKPIs(from, to, filters),
  this.calculateCharts(from, to, filters),
  this.calculateAlerts(from, filters)
]);

// ✅ Yaxshi - Faqat kerakli maydonlar
const visits = await this.prisma.visit.findMany({
  select: { 
    id: true, 
    status: true, 
    total_amount: true,
    visit_date: true
  },
  where: { 
    visit_date: { gte: from, lt: to },
    deleted_at: null
  }
});

// ✅ Yaxshi - Indexlardan foydalanish
const visits = await this.prisma.visit.findMany({
  where: { 
    visit_date: {
      gte: from,
      lt: to
    },
    deleted_at: null
  }
  // Uses index: @@index([visit_date, deleted_at])
});

// ❌ Yomon - Barcha maydonlar
const visits = await this.prisma.visit.findMany({
  where: { 
    visit_date: { gte: from, lt: to }
  }
});
```

### 8.2 Performance Talablari (Reference: `Klinika.md` 9.1)

| Ko'rsatkich | Qiymat | Measurement |
|-------------|--------|-------------|
| Dashboard Load Time | < 2 seconds | P95 |
| Cache Hit Rate | > 80% | Daily average |
| Export Generation Time | < 10 seconds | P95 |
| API Response Time | < 500ms (cached) | P95 |
| Concurrent Users | 50+ | Peak load |
| Data Freshness | 5 minutes | Cache TTL |

### 8.3 Database Indexlar (Reference: `klinika_prisma.txt`)

```prisma
// Visit
@@index([visit_date])              // Sana filter uchun (eng muhim)
@@index([status, visit_date])      // Status va sana uchun
@@index([client_id, visit_date])   // Mijoz va sana uchun
@@index([doctor_id, visit_date])   // Shifokor va sana uchun
@@index([visit_date, deleted_at])  // Sana va soft delete uchun

// Payment
@@index([payment_date])             // Sana filter uchun
@@index([payment_type, payment_date]) // Type va sana uchun
@@index([client_id, payment_date])  // Mijoz va sana uchun

// Client
@@index([created_at])               // Ro'yxatdan o'tgan sana uchun
@@index([status])                   // Status filter uchun

// Room
@@index([status])                   // Status filter uchun
@@index([record_status])            // Record status filter uchun
```

---

## 9. TEST TALABLARI

### 9.1 Unit Test Coverage (Reference: `Klinika.md` 9.1)

| Test Type | Minimum Coverage | Priority |
|-----------|-----------------|----------|
| Service Layer | 90% | 🔴 High |
| Controller Layer | 80% | 🟡 Medium |
| Cache Logic | 95% | 🔴 High |
| Integration | 70% | 🟡 Medium |

### 9.2 Test Cases

```typescript
// dashboard.service.spec.ts
describe('DashboardService', () => {
  let service: DashboardService;
  let prisma: PrismaService;
  let cacheService: CacheService;

  beforeEach(async () => {
    const module: TestingModule = await Test.createTestingModule({
      providers: [DashboardService, PrismaService, CacheService],
    }).compile();

    service = module.get<DashboardService>(DashboardService);
    prisma = module.get<PrismaService>(PrismaService);
    cacheService = module.get<CacheService>(CacheService);
  });

  describe('getDashboardMetrics', () => {
    it('should return dashboard metrics from cache', async () => {
      const cachedMetrics = { kpis: { totalVisits: 1350 } };
      
      cacheService.get = jest.fn().mockResolvedValue(cachedMetrics);
      
      const result = await service.getDashboardMetrics('month', undefined, 1, 'Admin');
      
      expect(result.cached).toBe(true);
      expect(cacheService.get).toHaveBeenCalled();
    });

    it('should calculate dashboard metrics if cache miss', async () => {
      cacheService.get = jest.fn().mockResolvedValue(null);
      prisma.visit.findMany = jest.fn().mockResolvedValue([]);
      cacheService.set = jest.fn().mockResolvedValue(null);
      
      const result = await service.getDashboardMetrics('month', undefined, 1, 'Admin');
      
      expect(result.cached).toBe(false);
      expect(prisma.visit.findMany).toHaveBeenCalled();
      expect(cacheService.set).toHaveBeenCalledWith(
        expect.stringContaining('dashboard'),
        expect.any(Object),
        { ttl: 300 }
      );
    });

    it('should filter by doctor role', async () => {
      const filters = service['getRoleFilters'](2, 'Doctor');
      
      expect(filters.doctor_id).toBe(2);
    });
  });

  describe('refreshDashboard', () => {
    it('should invalidate cache successfully', async () => {
      cacheService.keys = jest.fn().mockResolvedValue(['dashboard:1', 'dashboard:2']);
      cacheService.del = jest.fn().mockResolvedValue(null);
      
      const result = await service.refreshDashboard(1);
      
      expect(result.refreshedAt).toBeDefined();
      expect(cacheService.del).toHaveBeenCalled();
    });
  });

  describe('exportDashboard', () => {
    it('should generate Excel export', async () => {
      const dto: ExportDashboardDto = {
        period: 'month',
        format: 'excel',
        includeCharts: true
      };
      
      service.getDashboardMetrics = jest.fn().mockResolvedValue({});
      
      const result = await service.exportDashboard(dto, 1, 'Admin');
      
      expect(result).toBeInstanceOf(Buffer);
    });
  });
});
```

---

## 10. MIGRATSIYA VA DEPLOYMENT

### 10.1 Prisma Migration

Dashboard alohida jadval emas, shuning uchun migration kerak emas.

```bash
# Hech qanday migration kerak emas
# Report mavjud ma'lumotlardan real-time hosil qilinadi
```

### 10.2 Redis Configuration

```bash
# .env
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password
DASHBOARD_CACHE_TTL=300  # 5 daqiqa
DASHBOARD_ALERTS_TTL=120  # 2 daqiqa
EXPORT_CACHE_TTL=86400  # 24 soat
```

### 10.3 Deployment Checklist

- [ ] Redis server konfiguratsiyasi
- [ ] Cache service integration
- [ ] Unit test coverage 90%+
- [ ] E2E test o'tkazildi
- [ ] API documentation yangilandi
- [ ] Security audit o'tkazildi (Reference: `Klinika.md` 8)
- [ ] Performance test o'tkazildi (Reference: `Klinika.md` 9.1)
- [ ] Cache invalidation test qilindi
- [ ] Export functionality test qilindi
- [ ] RBAC test qilindi
- [ ] Large dataset test qilindi (10000+ records)

---

## 11. BOG'LIQ HUJJATLAR

| Hujjat | Link | Status |
|--------|------|--------|
| Flow Document | `25-dashboard-metrics-management.md` | ✅ Tasdiqlandi |
| Prisma Schema | `klinika_prisma.txt` | ✅ Tasdiqlandi |
| Tech Spec | `Klinika.md` | ✅ Tasdiqlandi |
| Daily Report RFC | `RFC-019-daily-report-management.md` | ✅ Tasdiqlandi |
| Monthly Report RFC | `RFC-020-monthly-report-management.md` | ✅ Tasdiqlandi |
| Doctor Performance RFC | `RFC-021-doctor-performance-management.md` | ✅ Tasdiqlandi |
| Client Report RFC | `RFC-022-client-report-management.md` | ✅ Tasdiqlandi |
| Service Report RFC | `RFC-023-service-report-management.md` | ✅ Tasdiqlandi |
| Debt Report RFC | `RFC-024-debt-report-management.md` | ✅ Tasdiqlandi |

---

## 12. QABUL QILISH MEZONLARI (ACCEPTANCE CRITERIA)

### 12.1 Functional Requirements

- [ ] Barcha KPI metrikalari to'g'ri hisoblanishi
- [ ] Grafiklar ma'lumotlari to'g'ri ishlashi
- [ ] Tezkor xabarlar to'g'ri ko'rsatilishi
- [ ] Rol bo'yicha filterlash ishlashi
- [ ] Vaqt oralig'i filtrlari ishlashi (today/week/month/year/custom)
- [ ] Cache strategiyasi ishlashi (5 daqiqa TTL)
- [ ] Export funksiyasi ishlashi (PDF/Excel)
- [ ] Dashboard refresh ishlashi
- [ ] Xatoliklar to'g'ri HTTP status va kod bilan qaytarilishi

### 12.2 Non-Functional Requirements (Reference: `Klinika.md` 9.1)

- [ ] Dashboard load time < 2 seconds
- [ ] API response time < 500ms (cached)
- [ ] Cache hit rate > 80%
- [ ] Export generation time < 10 seconds
- [ ] Unit test coverage ≥ 90%
- [ ] E2E test coverage ≥ 70%
- [ ] Security audit o'tkazildi (Reference: `Klinika.md` 8)
- [ ] Documentation to'liq
- [ ] Concurrent users 50+ qo'llab-quvvatlanadi
- [ ] Data retention 5 yil

---

## 13. RISKLAR VA YECHIMLAR (Reference: `Klinika.md` 8)

| Risk | Ehtimollik | Ta'sir | Yechim |
|------|------------|--------|--------|
| Performance degradation | O'rta | Yuqori | Caching + parallel queries |
| Cache inconsistency | O'rta | O'rta | Event-based invalidation |
| Large data volume | Past | O'rta | Pagination + date range limit |
| Export timeout | Past | O'rta | Background job + notification |
| RBAC bypass | Past | Yuqori | Middleware + unit tests |
| Data accuracy | O'rta | Yuqori | Validation + audit logs |
| Cache memory usage | Past | O'rta | TTL + LRU eviction |
| Real-time updates | Past | O'rta | WebSocket (kelajakda) |

---

## 14. KELAJAKDA QO'SHILISHI MUMKIN

| Feature | Priority | Phase |
|---------|----------|-------|
| Real-time WebSocket updates | 🟡 Medium | Phase 4 |
| Custom dashboard builder | 🟢 Low | Phase 4 |
| Automated email reports | 🟡 Medium | Phase 4 |
| Mobile app dashboard | 🟢 Low | Phase 4 |
| AI-powered insights | 🟢 Low | Phase 5 |
| Comparative analytics | 🟢 Low | Phase 4 |
| Goal tracking | 🟢 Low | Phase 4 |
| Multi-clinic comparison | 🟢 Low | Phase 4 |

---

## 15. TASDIQLASH

| Rol | Ism | Imzo | Sana |
|-----|-----|------|------|
| **Author** | Senior Backend Developer | __________ | _________ |
| **Reviewer** | Tech Lead | __________ | _________ |
| **Approver** | Project Manager | __________ | _________ |
| **Security Audit** | Security Engineer | __________ | _________ |
| **Business Owner** | Clinic Director | __________ | _________ |

---

**RFC Versiyasi:** 1.0
**Status:** Draft
**Oxirgi Yangilanish:** 2024-01-15
**Reference Documents:** `klinika_prisma.txt`, `Klinika.md` (Sections 7.1, 7.2, 8.1, 9.1)

---

## 📋 PHASE 3 - YAKUNLANDI! 🎉

| # | Model | Flow | RFC | Status |
|---|-------|------|-----|--------|
| 19 | `Daily Report` | ✅ | ✅ RFC-019 | ✅ Complete |
| 20 | `Monthly Report` | ✅ | ✅ RFC-020 | ✅ Complete |
| 21 | `Doctor Performance` | ✅ | ✅ RFC-021 | ✅ Complete |
| 22 | `Client Report` | ✅ | ✅ RFC-022 | ✅ Complete |
| 23 | `Service Report` | ✅ | ✅ RFC-023 | ✅ Complete |
| 24 | `Debt Report` | ✅ | ✅ RFC-024 | ✅ Complete |
| 25 | `Dashboard Metrics` | ✅ | ✅ **RFC-025** | ✅ **Complete** |

**Phase 3: 7/7 Complete (100%)** 🎉

---
